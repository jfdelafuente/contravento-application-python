# ============================================================================
# ContraVento - Jenkins CI/CD Environment
# ============================================================================
# Standalone Docker Compose file for CI/CD pipelines (Jenkins, GitHub Actions)
# Uses pre-built images from Docker Hub (built by docker-build-push.yml workflow)
#
# Includes all services needed for testing and validation:
#   - PostgreSQL (database)
#   - Backend API (FastAPI) - FROM jfdelafuente/contravento-backend:latest
#   - Frontend (Nginx/React) - FROM jfdelafuente/contravento-frontend:latest
#   - pgAdmin (database UI for debugging)
#
# This is a self-contained file (no overlays needed) optimized for:
#   - Jenkins pipelines
#   - GitHub Actions workflows
#   - CI/CD automated testing
#   - Quick spin-up/teardown cycles
#   - NO BUILD REQUIRED - Uses pre-built Docker Hub images
#
# Usage:
#   docker-compose -f docker-compose-jenkins.yml up -d
#   docker-compose -f docker-compose-jenkins.yml down
#
# Environment variables (auto-generated if not provided):
#   SECRET_KEY:           Auto-generated if missing
#   POSTGRES_PASSWORD:    Defaults to 'jenkins_test_password'
#   TURNSTILE_SITE_KEY:   Defaults to test key (1x00000000000000000000AA)
#
# Access:
#   Frontend:           http://localhost:80
#   Backend API:        http://localhost:8000
#   API Docs:           http://localhost:8000/docs
#   pgAdmin:            http://localhost:5050 (admin@jenkins.local / jenkins)
#   PostgreSQL:         localhost:5432 (contravento_ci / postgres / jenkins_test_password)
# ============================================================================

version: '3.8'

services:
  # ==========================================================================
  # PostgreSQL - CI/CD Database
  # ==========================================================================
  postgres:
    image: postgres:16-alpine
    container_name: contravento-db-jenkins
    environment:
      POSTGRES_DB: contravento_ci
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-jenkins_test_password}
      POSTGRES_INITDB_ARGS: --encoding=UTF8 --locale=en_US.UTF-8
    ports:
      - "5432:5432"  # Expose for local access
    volumes:
      - postgres_data_jenkins:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d contravento_ci"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    restart: unless-stopped
    networks:
      - jenkins-network

  # ==========================================================================
  # Backend API - FastAPI
  # ==========================================================================
  backend:
    image: jfdelafuente/contravento-backend:latest
    container_name: contravento-api-jenkins
    environment:
      # Application
      APP_NAME: ContraVento
      APP_ENV: ci
      DEBUG: "true"

      # Database
      DATABASE_URL: postgresql+asyncpg://postgres:${POSTGRES_PASSWORD:-jenkins_test_password}@postgres:5432/contravento_ci

      # Security (auto-generate SECRET_KEY if not provided)
      SECRET_KEY: ${SECRET_KEY:-jenkins_default_secret_key_for_testing_only_do_not_use_in_production}
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 15
      REFRESH_TOKEN_EXPIRE_DAYS: 30
      BCRYPT_ROUNDS: 4  # Fast hashing for CI/CD

      # SMTP (disabled for CI/CD)
      SMTP_HOST: ""
      SMTP_PORT: "1025"
      SMTP_FROM: noreply@contravento.local

      # CORS
      CORS_ORIGINS: http://localhost:5173,http://localhost:8000

      # Storage
      STORAGE_PATH: /app/storage

      # Logging
      LOG_LEVEL: INFO
      LOG_FORMAT: text  # Human-readable for CI logs

    ports:
      - "8000:8000"
    volumes:
      # Mount local storage for uploads (persistent across container restarts)
      - ./backend/storage:/app/storage
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - jenkins-network

  # ==========================================================================
  # Frontend - React/Vite Production Build
  # ==========================================================================
  frontend:
    image: jfdelafuente/contravento-frontend:latest
    container_name: contravento-frontend-jenkins
    # NOTE: Frontend image is production build (Nginx serving static files)
    # VITE_* variables were embedded at build-time in docker-build-push.yml
    # No environment variables needed at runtime for static content
    ports:
      - "80:80"  # Nginx serves on port 80
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    restart: unless-stopped
    networks:
      - jenkins-network

  # ==========================================================================
  # pgAdmin - Database UI (for debugging CI failures)
  # ==========================================================================
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: contravento-pgadmin-jenkins
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@jenkins.local
      PGADMIN_DEFAULT_PASSWORD: jenkins
      PGADMIN_CONFIG_SERVER_MODE: "False"
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data_jenkins:/var/lib/pgadmin
    depends_on:
      - postgres
    restart: unless-stopped
    networks:
      - jenkins-network
    # Auto-configure PostgreSQL connection
    configs:
      - source: pgadmin_servers
        target: /pgadmin4/servers.json

# ============================================================================
# Volumes
# ============================================================================
volumes:
  postgres_data_jenkins:
    driver: local
  pgadmin_data_jenkins:
    driver: local

# ============================================================================
# Networks
# ============================================================================
networks:
  jenkins-network:
    driver: bridge
    name: jenkins-network

# ============================================================================
# Configs (pgAdmin auto-configuration)
# ============================================================================
configs:
  pgadmin_servers:
    content: |
      {
        "Servers": {
          "1": {
            "Name": "ContraVento CI Database",
            "Group": "Jenkins",
            "Host": "postgres",
            "Port": 5432,
            "MaintenanceDB": "contravento_ci",
            "Username": "postgres",
            "SSLMode": "prefer",
            "PassFile": "/tmp/pgpass"
          }
        }
      }

# ============================================================================
# Usage Examples
# ============================================================================
# 1. Start all services:
#    docker-compose -f docker-compose-jenkins.yml up -d
#
# 2. View logs:
#    docker-compose -f docker-compose-jenkins.yml logs -f backend
#    docker-compose -f docker-compose-jenkins.yml logs -f frontend
#
# 3. Run backend tests:
#    docker-compose -f docker-compose-jenkins.yml exec backend pytest
#
# 4. Run frontend tests:
#    docker-compose -f docker-compose-jenkins.yml exec frontend npm test
#
# 5. Stop all services:
#    docker-compose -f docker-compose-jenkins.yml down
#
# 6. Clean up everything (including volumes):
#    docker-compose -f docker-compose-jenkins.yml down -v
#
# ============================================================================
# Jenkins Pipeline Integration
# ============================================================================
# Jenkinsfile example:
#
#   pipeline {
#     agent any
#     stages {
#       stage('Setup') {
#         steps {
#           sh 'docker-compose -f docker-compose-jenkins.yml up -d'
#           sh 'sleep 30'  // Wait for services to be healthy
#         }
#       }
#       stage('Test Backend') {
#         steps {
#           sh 'docker-compose -f docker-compose-jenkins.yml exec -T backend pytest --cov=src'
#         }
#       }
#       stage('Test Frontend') {
#         steps {
#           sh 'docker-compose -f docker-compose-jenkins.yml exec -T frontend npm test'
#         }
#       }
#       stage('E2E Tests') {
#         steps {
#           sh 'docker-compose -f docker-compose-jenkins.yml exec -T frontend npm run test:e2e'
#         }
#       }
#     }
#     post {
#       always {
#         sh 'docker-compose -f docker-compose-jenkins.yml down -v'
#       }
#     }
#   }
# ============================================================================
