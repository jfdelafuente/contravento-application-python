# =====================================================
# Staging Environment Configuration (EXAMPLE)
# =====================================================
#
# USAGE:
# 1. Copy this file: cp .env.staging.example .env.staging
# 2. Replace placeholder values with actual staging credentials
# 3. NEVER commit .env.staging to git (already in .gitignore)
# 4. Run: npm run build:staging
#
# =====================================================

# Backend API URL (staging environment)
# Replace with your actual staging API URL
VITE_API_URL=https://api-staging.contravento.com

# Cloudflare Turnstile site key (staging)
# Get from: https://dash.cloudflare.com/?to=/:account/turnstile
# Replace with your staging Turnstile site key
VITE_TURNSTILE_SITE_KEY=your_staging_turnstile_site_key_here

# Environment
VITE_ENV=staging

# Debug mode (enabled for staging to help debugging)
VITE_DEBUG=true
