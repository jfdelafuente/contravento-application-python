# =====================================================
# Production Environment Configuration (EXAMPLE)
# =====================================================
#
# USAGE:
# 1. Copy this file: cp .env.production.example .env.production
# 2. Replace placeholder values with actual production credentials
# 3. NEVER commit .env.production to git (already in .gitignore)
# 4. Run: npm run build:prod
#
# ⚠️  WARNING: Production credentials are sensitive!
# Store them securely (password manager, CI/CD secrets)
#
# =====================================================

# Backend API URL (production environment)
# Replace with your actual production API URL
VITE_API_URL=https://api.contravento.com

# Cloudflare Turnstile site key (production)
# Get from: https://dash.cloudflare.com/?to=/:account/turnstile
# Replace with your production Turnstile site key
VITE_TURNSTILE_SITE_KEY=your_production_turnstile_site_key_here

# Environment
VITE_ENV=production

# Debug mode (MUST be disabled for production security)
VITE_DEBUG=false
