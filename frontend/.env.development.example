# =====================================================
# Development Environment Configuration (EXAMPLE)
# =====================================================
#
# USAGE:
# 1. Copy this file: cp .env.development.example .env.development
# 2. The default values work out-of-the-box for local development
# 3. NEVER commit .env.development to git (already in .gitignore)
# 4. Run: npm run dev
#
# =====================================================

# Backend API URL (local development)
VITE_API_URL=http://localhost:8000

# Cloudflare Turnstile site key (development)
# This is a test key that always passes (safe for development)
VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA

# Environment
VITE_ENV=development

# Debug mode (enabled for development)
VITE_DEBUG=true
